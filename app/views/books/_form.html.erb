<%= form_for(@book) do |f| %>
  <% if @book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
      <% @book.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :isbn, titleize(t(:isbn)) %><br>
    <%= f.text_field :isbn %>
  </div>
  <div class="field">
    <%= f.label :title, titleize(t(:title)) %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :author, titleize(t(:author)) %><br>
    <%= f.number_field :author_id %>
  </div>
  <div class="field">
    <%= f.label :language, titleize(t(:language)) %><br>
    <%= f.text_field :language %>
  </div>
  <div class="field">
    <%= f.label :description, titleize(t(:description)) %><br>
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :photo, titleize(t(:photo)) %><br>
    <%= f.text_field :photo %>
  </div>
  <div class="field">
    <%= f.label :quantity, titleize(t(:quantity)) %><br>
    <%= f.number_field :quantity %>
  </div>
  <div class="field">
    <%= f.label :place, titleize(t(:place)) %><br>
    <%= f.text_field :place %>
  </div>
  <div class="field">
    <%= f.label :release_date, titleize(t(:release_date)) %><br>
    <%= f.date_select :release_date, {:include_blank => true, :default => nil} %>
  </div>
  <div class="field">
    <%= f.label :pages, titleize(t(:pages)) %><br>
    <%= f.number_field :pages %>
  </div>
  <div class="actions">
    <%= f.submit titleize(t(:save))%>
  </div>
<% end %>
