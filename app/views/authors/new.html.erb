<% provide(:page_title, titleize(t(:add_author))) %>
<div class="well">
<div class="page-header">
    <h1><%= titleize(t(:add_author))%></h1>
</div>

<%= render 'form' %>

<%= link_to titleize(t(:back)), authors_path %>
</div>
