<% provide(:page_title, titleize(t(:edit_author))) %>
<div class="well">
<div class="page-header">
    <h1><%= titleize(t(:edit_author)) %></h1>
</div>

<%= render 'form' %>

<%= link_to titleize(t(:back)), author_path %>
</div>
