<% provide(:page_title, titleize(t(:edit_author))) %>
<h1><%= titleize(t(:edit_author))%></h1>

<%= render 'form' %>

<%= link_to titleize(t(:show)), @author %> |
<%= link_to titleize(t(:back)), authors_path %>
