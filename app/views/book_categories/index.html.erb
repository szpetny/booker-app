<% provide(:page_title, titleize(t(:book_categories))) %>
<div class="page-header">
    <h1><%= titleize(t(:book_categories))%></h1>
</div>
<div class="well">

    <ul class="people">
      <%= render @book_categories %>
    </ul>

</div>
